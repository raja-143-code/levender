{
  "name": "lavender-park-beauty-parlour",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "mkdir -p dist && cp -r index.html aari-class.html blog.html blog img js style robots.txt sitemap.xml site.webmanifest favicon.ico favicon.svg dist/ 2>/dev/null || true",
    "start": "node server.js"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "sharp": "^0.35.4"
  }
}
